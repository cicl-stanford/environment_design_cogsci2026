# Data

Processed behavioral and model outputs organized by study.

## Structure

```
data/
├── s1_design_inference/
│   ├── behavioral_results/
│   │   ├── trial_data.csv            # One row per slider response
│   │   └── session_data.csv          # One row per participant session
│   └── models/
│       └── model_results.csv         # Simulation metrics per trial x config
│
└── overcooked_design/
    ├── behavioral_results/
    │   ├── design_data.csv           # One row per design trial
    │   └── session_data.csv          # One row per participant session
    ├── model_results/
    │   ├── planning_model_outputs.csv  # Raw simulation outputs (aggregated from cluster)
    │   └── placement_utilities.csv   # Model utilities per placement x config x dish
    └── model_cache/
        ├── brms_fit_*.rds            # Bayesian regression model fits (8 files)
        ├── loo_*.rds                 # LOO cross-validation objects (9 files)
        ├── split_half_accuracy_ceiling.rds  # Human split-half ceiling
        ├── h0a_results.rds           # Hypothesis test results
        └── h0b_results.rds           # Hypothesis test results
```

## Data Sources

**Behavioral data:** Retrieved from OSF using `get_behavioral_results.py` scripts. Each script downloads raw experimental data and processes it into structured CSV files.

- Study 1 OSF component: `ykeqp` (inference), `8fjh9` (effort)
- Study 2 OSF component: `ywtf5`

**Model data (Study 1):** Generated by running gym-cooking simulations via `model_runs.py`. The `process_model_outputs.py` script converts raw simulation outputs (pickle files) into the analysis-ready CSV.

**Model data (Study 2):** The simulation pipeline produces data in two stages:
1. `planning_model_outputs.csv` — raw simulation outputs aggregated from per-combination CSVs produced on Sherlock (see `bash/overcooked_design/aggregate_csvs.sh`)
2. `placement_utilities.csv` — model utilities computed from the raw outputs by `get_placement_utilities.py`, with columns for all four model variants (full, oracle, inference ablation, efficiency ablation)

**Model cache (Study 2):** Pre-computed objects that are expensive to regenerate. Can be downloaded from [OSF](https://osf.io/download/5vcyk/).

## Study 1: trial_data.csv Schema

| Column | Description |
|---|---|
| trial_id | Unique trial identifier (UUID) |
| response_id | Unique response identifier (UUID) |
| game_id | Participant session identifier |
| kitchen_layout_id | Kitchen trial ID (trial_01 through trial_36) |
| trial_order | Presentation order within session |
| condition | Experimental condition (cooks, dish, cooks_effort, dish_effort) |
| question_type | Type of slider question |
| slider_response | Participant's slider value (0-100) |
| rt | Response time in milliseconds |

## Study 1: model_results.csv Schema

| Column | Description |
|---|---|
| trial_id | Kitchen trial ID |
| num_agents | Number of agents (1 or 2) |
| model_type | Planning algorithm (greedy or bd) |
| recipe | Dish type (Salad or SaladOL) |
| seed | Random seed |
| timesteps | Steps to complete task |

## Study 2: design_data.csv Schema

| Column | Description |
|---|---|
| trial_id | Unique trial identifier (UUID) |
| game_id | Participant session identifier |
| trial_num | Global trial number |
| block_num | Block number |
| trial_num_in_block | Trial number within block |
| config_filename | Kitchen configuration JSON filename |
| dish | Target dish (SaladOL or Salad) |
| floor_plan | ASCII kitchen layout (before furniture placement) |
| final_layout | ASCII kitchen layout (after furniture placement) |
| furniture_placed | Whether furniture was placed (boolean) |
| total_placed | Number of furniture pieces placed |
| placements | JSON string encoding placement coordinates and metadata |
| undo_count | Number of undo actions |
| redo_count | Number of redo actions |
| reset_count | Number of reset actions |
| rt | Response time in milliseconds |
| actions | JSON string encoding the full action sequence |
| design_space | Design space specification |
| design_intent | Design intent condition for this trial |

## Study 2: session_data.csv Schema

| Column | Description |
|---|---|
| game_id | Participant session identifier |
| project | Project name |
| experiment | Experiment name |
| iteration | Experiment iteration |
| dev_mode | Whether session was in dev mode |
| condition | Experimental condition (base or communicative) |
| randomization | Randomization seed |
| prolific_id | Prolific participant ID |
| browser | Browser name |
| browser_width | Browser window width |
| browser_height | Browser window height |
| is_mobile_device | Whether participant used a mobile device |
| start_experiment_ts | Experiment start timestamp |
| end_experiment_ts | Experiment end timestamp |
| experiment_duration_ms | Total experiment duration in milliseconds |
| comprehension_attempts | Number of comprehension check attempts |
| design_trial_order | Order of design trials |
| age | Participant age |
| gender | Participant gender |
| race | Participant race |
| ethnicity | Participant ethnicity |
| judged_difficulty | Self-reported difficulty rating |
| judged_effort | Self-reported effort rating |
| input_device | Input device used |
| feedback | Free-text feedback |
| technical_difficulties | Reported technical difficulties |

## Study 2: placement_utilities.csv Schema

| Column | Description |
|---|---|
| config_id | Kitchen configuration UUID |
| dish | Dish name (TomatoSalad or OnionSalad) |
| placement_id | Furniture placement index (0-11) |
| placement_x | Placement x coordinate |
| placement_y | Placement y coordinate |
| cost_g | Simulation cost for intended dish |
| cost_not_g | Simulation cost for alternative dish |
| p_cook | Cook's inference probability P(g) |
| lambda_param | Inference sensitivity parameter |
| max_timesteps | Feasibility threshold |
| U_full | Full model utility |
| U_oracle | Oracle model utility |
| U_inference_ablation | Inference ablation utility |
| U_efficiency_ablation | Efficiency ablation utility |
